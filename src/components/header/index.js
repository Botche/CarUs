import React from 'react';

import Navigation from '../navigation';

function Header(props) {
    return (
        <header>
            <Navigation />
        </header>
    );
}

export default Header;