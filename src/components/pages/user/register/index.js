import React from 'react';

import Layout from '../../../layout';

function Register(props) {
    return (
      <Layout>    
        <p>Register</p>
      </Layout>
    );
}

export default Register;